import { BrowserRouter } from "react-router-dom";

import AllRoutes from "./AllRoutes";

const Routes = () => {
  return (
    <BrowserRouter>
      <AllRoutes />
    </BrowserRouter>
  );
};

export default Routes;
